{
  "name": "redux-action-router",
  "version": "1.1.0",
  "description": "A redux middleware that implements a simple, mapping between url routes and action creators.",
  "main": "dist/reduxActionRouter.cjs.js",
  "module": "dist/reduxActionRouter.esm.js",
  "repository": "github:itaylor/redux-action-router",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "node node_modules/.bin/rollup -c",
    "dev": "node node_modules/.bin/rollup -c -w",
    "test": "NODE_ENV=test node node_modules/.bin/jest",
    "pretest": "yarn run build",
    "coverage": "node node_modules/.bin/jest --coverage",
    "precoverage": "yarn run build"
  },
  "keywords": [
    "redux",
    "middleware",
    "action",
    "route",
    "routing",
    "thunk"
  ],
  "author": "ian.b.taylor@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "jest": "^24.8.0",
    "redux": "^4.0.1",
    "redux-thunk": "^2.3.0",
    "rollup": "^1.15.3"
  },
  "dependencies": {
    "url-mapper": "^2.0.0"
  }
}
